# Environment record

Fill this before collecting final measurements. Runtime metadata is saved per run.

- Host machine and OS:
- QEMU version and exact launch command:
- Acceleration used (KVM/TCG/other, verified how):
- vCPU count and virtual CPU/PMU settings:
- Host CPU affinity, other load, power/turbo settings:
- Guest CPU affinity (config.json):
- Guest memory:
- Any perf permissions or watchdog changes (before and after):
- Why this interpreter/version was selected:

Do not infer acceleration mode or PMU capability from a kernel name alone.
