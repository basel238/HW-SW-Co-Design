.venv/
.venv-before-debug-*/
.venv-guide/
.course-cache/
__pycache__/
*.pyc
results/**/perf.data*
results/**/native-stacks.txt
results/**/*.pstats
results/**/source/
results/**/*.ppm
results/**/*.fifo
results/**/jitted-*.so
results/**/evidence.zip
# Keep project sources, metadata, JSON, readable reports, folded stacks and SVGs.
# Per-run source snapshots and raw traces above are local: use tools/export_results.py
# to preserve them outside Git before cleanup or sharing a complete evidence bundle.
